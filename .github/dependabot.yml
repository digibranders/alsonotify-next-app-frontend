version: 2
updates:
  # Only GitHub Actions updates (low volume) — npm version bumps removed
  # Security vulnerability PRs are handled automatically by GitHub's Dependabot security updates
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    open-pull-requests-limit: 3
