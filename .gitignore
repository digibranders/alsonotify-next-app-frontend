# Dependencies
/node_modules
/.pnp
.pnp.js

# Testing
/coverage
guidelines/
# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local Env Files
.env
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

alsonotify-new-ui/refactor_report.md

# Bun
bun.lock
bun.lockb

refactor_report.md

# Local runs
*.log
*.pid
*.zip

# Sentry Config File
# Sentry build-plugin token — MUST NEVER be tracked.
# Was committed 3x before (0d5640b, 07c5443, bd2aa77) and kept returning:
# a .gitignore entry does nothing for an already-tracked file, so those
# "removal" commits never actually untracked it. Untracked for real in #393.
# CI/CD should read SENTRY_AUTH_TOKEN from GitHub Actions / Vercel env vars.
.env.sentry-build-plugin

# This project uses pnpm exclusively (see pnpm-lock.yaml).
# A stray package-lock.json resolves a different tree than CI and production.
package-lock.json
bundle-report-*.txt
